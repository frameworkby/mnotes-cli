/**
 * Template for Claude Code session hooks.
 * Generates bash scripts in `~/.claude/hooks/mnotes/scripts/` (user-global,
 * namespaced under `mnotes`) and hook entries for `.claude/settings.json`
 * (project-local). Generated by m-notes CLI — do not edit manually.
 */

import * as os from "os";
import * as path from "path";

/**
 * Absolute path where m-notes hook scripts live (user-global, namespaced).
 * Resolved via function so tests can override `HOME` to point to a tmp dir.
 */
export function getHookScriptsDir(): string {
  return path.join(os.homedir(), ".claude", "hooks", "mnotes", "scripts");
}

/** @deprecated kept for backward compatibility — use `getHookScriptsDir()` so HOME overrides work. */
export const HOOK_SCRIPTS_DIR = getHookScriptsDir();

export interface HooksTemplateOpts {
  url: string;
  workspaceId: string; // written to MNOTES_WORKSPACE_ID in generated scripts
}

export interface ClaudeCodeHook {
  type: string;
  command: string;
}

export interface ClaudeCodeHookEntry {
  matcher: string;
  hooks: ClaudeCodeHook[];
}

export interface ClaudeCodeHooks {
  SessionStart?: ClaudeCodeHookEntry[];
  [key: string]: ClaudeCodeHookEntry[] | undefined;
}

export interface HookScript {
  filename: string;
  content: string;
}

/**
 * Generates bash scripts to be written to `~/.claude/hooks/mnotes/scripts/`.
 * Workspace is resolved at runtime from MNOTES_WORKSPACE_ID (set by the hook
 * command), the per-cwd config map, or the global config default — no
 * --workspace-id flag is passed.
 */
export function generateHookScripts(_opts: HooksTemplateOpts): HookScript[] {
  const sessionStartScript = `#!/usr/bin/env bash
# Generated by m-notes CLI — do not edit manually.
# Loads project context on Claude Code session start and emits it as
# additionalContext via the SessionStart hook JSON envelope so Claude Code
# injects it into the session.
set -euo pipefail

if [ -z "\${MNOTES_WORKSPACE_ID:-}" ]; then
  echo 'mnotes-session-start: MNOTES_WORKSPACE_ID not set — workspace will be resolved from config' >&2
fi

# Capture output; suppress stderr to avoid noisy session starts.
# On any failure (network, missing workspace, CLI not installed) exit 0 silently
# so the hook never blocks Claude Code from starting.
_context=\$(mnotes composite project-load --query "session start" 2>/dev/null) || true

if [ -n "\$_context" ]; then
  # Wrap in the SessionStart hook JSON envelope so Claude Code injects the
  # context as additionalContext for the session.
  # Uses jq when available for safe JSON encoding; falls back to printf.
  if command -v jq >/dev/null 2>&1; then
    printf '%s' "\$_context" | jq -Rs '{hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:.}}'
  else
    # Minimal fallback: escape backslashes and double-quotes only.
    _escaped=\$(printf '%s' "\$_context" | sed 's/\\\\/\\\\\\\\/g; s/"/\\\\"/g; s/$/\\\\n/g' | tr -d '\\n')
    printf '{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"%s"}}\\n' "\$_escaped"
  fi
fi
`;

  const sessionStopScript = `#!/usr/bin/env bash
# Generated by m-notes CLI — do not edit manually.
# Logs session end to m-notes.
# Workspace is resolved from MNOTES_WORKSPACE_ID env var, per-cwd config map,
# or global config default (set via \`mnotes workspace select\`).
set -euo pipefail

SESSION_ID="\${MNOTES_SESSION_ID:-$(date +%Y%m%d-%H%M%S)-$$}"

# stderr is suppressed (2>&1) to keep session shutdown clean — Stop hook
# output is not injected as context, so noisy errors have no benefit here.
mnotes session log \\
  --session-id "$SESSION_ID" \\
  --summary "Session ended" > /dev/null 2>&1 || true
`;

  return [
    { filename: "mnotes-session-start.sh", content: sessionStartScript },
    { filename: "mnotes-session-stop.sh", content: sessionStopScript },
  ];
}

/**
 * Generates the hooks object to merge into `.claude/settings.json`.
 * References bash scripts in `~/.claude/hooks/mnotes/scripts/` (absolute path
 * so hooks work across projects without per-project script copies).
 * Workspace ID is exported as MNOTES_WORKSPACE_ID so the CLI resolver picks it up.
 */
export function generateHooksTemplate(opts: HooksTemplateOpts): ClaudeCodeHooks {
  const dir = getHookScriptsDir();
  const startScript = path.join(dir, "mnotes-session-start.sh");
  const stopScript = path.join(dir, "mnotes-session-stop.sh");
  return {
    SessionStart: [
      {
        matcher: "",
        hooks: [
          {
            type: "command",
            command: `MNOTES_WORKSPACE_ID=${opts.workspaceId} ${startScript}`,
          },
        ],
      },
    ],
    Stop: [
      {
        matcher: "",
        hooks: [
          {
            type: "command",
            command: `MNOTES_WORKSPACE_ID=${opts.workspaceId} ${stopScript}`,
          },
        ],
      },
    ],
  };
}

/** Header comment for the hooks section, used for identification. */
export const HOOKS_HEADER = "m-notes: auto-generated hooks";
