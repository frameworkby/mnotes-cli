import type { ActionDescriptor } from "../_register-group";
interface Input {
    client?: string;
    baseUrl?: string;
}
export declare const instructionsAction: ActionDescriptor<Input, unknown>;
export {};
