import type { ActionDescriptor } from "../_register-group";
interface Input {
    id: string;
    maxLength?: number;
}
export declare const noteSummaryAction: ActionDescriptor<Input, unknown>;
export {};
