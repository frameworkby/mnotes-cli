import type { ActionDescriptor } from "../_register-group";
interface Input {
    title: string;
    workspaceId?: string;
}
export declare const byTitleAction: ActionDescriptor<Input, unknown>;
export {};
