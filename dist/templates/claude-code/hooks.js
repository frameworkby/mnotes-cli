"use strict";
/**
 * Template for Claude Code session hooks.
 * Generates bash scripts in `~/.claude/hooks/mnotes/scripts/` (user-global,
 * namespaced under `mnotes`) and hook entries for `.claude/settings.json`
 * (project-local). Generated by m-notes CLI — do not edit manually.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.HOOKS_HEADER = exports.HOOK_SCRIPTS_DIR = void 0;
exports.getHookScriptsDir = getHookScriptsDir;
exports.generateHookScripts = generateHookScripts;
exports.generateHooksTemplate = generateHooksTemplate;
const os = __importStar(require("os"));
const path = __importStar(require("path"));
/**
 * Absolute path where m-notes hook scripts live (user-global, namespaced).
 * Resolved via function so tests can override `HOME` to point to a tmp dir.
 */
function getHookScriptsDir() {
    return path.join(os.homedir(), ".claude", "hooks", "mnotes", "scripts");
}
/** @deprecated kept for backward compatibility — use `getHookScriptsDir()` so HOME overrides work. */
exports.HOOK_SCRIPTS_DIR = getHookScriptsDir();
/**
 * Generates bash scripts to be written to `.claude/hooks/`.
 */
function generateHookScripts(opts) {
    const baseUrl = opts.url.replace(/\/+$/, "");
    const mcpUrl = `${baseUrl}/api/mcp`;
    function mcpPayload(method, args) {
        return JSON.stringify({
            jsonrpc: "2.0",
            method: "tools/call",
            params: { name: method, arguments: args },
            id: 1,
        });
    }
    const sessionStartScript = `#!/usr/bin/env bash
# Generated by m-notes CLI — do not edit manually.
# Loads project context on Claude Code session start.
# Usage: mnotes-session-start.sh <workspaceId>
set -euo pipefail

WORKSPACE_ID="\${1:-\${MNOTES_WORKSPACE_ID:-}}"
if [ -z "$WORKSPACE_ID" ]; then
  echo "mnotes-session-start.sh: no workspaceId (pass as \$1 or set MNOTES_WORKSPACE_ID)" >&2
  exit 0
fi
MNOTES_URL="${mcpUrl}"
PAYLOAD="$(echo '${mcpPayload("project_context_load", { workspaceId: "__WORKSPACE_ID__", query: "session start" })}' | sed "s/__WORKSPACE_ID__/$WORKSPACE_ID/")"

curl -s \\
  -H "Authorization: Bearer $MNOTES_API_KEY" \\
  -H "Content-Type: application/json" \\
  -H "Accept: text/event-stream" \\
  -H "Accept: application/json" \\
  "$MNOTES_URL" \\
  -d "$PAYLOAD" > /dev/null 2>&1 || true
`;
    const sessionStopScript = `#!/usr/bin/env bash
# Generated by m-notes CLI — do not edit manually.
# Logs session end to m-notes.
# Usage: mnotes-session-stop.sh <workspaceId>
set -euo pipefail

WORKSPACE_ID="\${1:-\${MNOTES_WORKSPACE_ID:-}}"
if [ -z "$WORKSPACE_ID" ]; then
  echo "mnotes-session-stop.sh: no workspaceId (pass as \$1 or set MNOTES_WORKSPACE_ID)" >&2
  exit 0
fi
MNOTES_URL="${mcpUrl}"
PAYLOAD="$(echo '${mcpPayload("session_log", { workspaceId: "__WORKSPACE_ID__", summary: "Session ended", decisions: [], actions: [] })}' | sed "s/__WORKSPACE_ID__/$WORKSPACE_ID/")"

curl -s \\
  -H "Authorization: Bearer $MNOTES_API_KEY" \\
  -H "Content-Type: application/json" \\
  -H "Accept: text/event-stream" \\
  -H "Accept: application/json" \\
  "$MNOTES_URL" \\
  -d "$PAYLOAD" > /dev/null 2>&1 || true
`;
    return [
        { filename: "mnotes-session-start.sh", content: sessionStartScript },
        { filename: "mnotes-session-stop.sh", content: sessionStopScript },
    ];
}
/**
 * Generates the hooks object to merge into `.claude/settings.json`.
 * References bash scripts in `~/.claude/hooks/mnotes/scripts/` (absolute path
 * so hooks work across projects without per-project script copies).
 */
function generateHooksTemplate(opts) {
    const dir = getHookScriptsDir();
    const startScript = path.join(dir, "mnotes-session-start.sh");
    const stopScript = path.join(dir, "mnotes-session-stop.sh");
    return {
        SessionStart: [
            {
                matcher: "",
                hooks: [
                    {
                        type: "command",
                        command: `${startScript} ${opts.workspaceId}`,
                    },
                ],
            },
        ],
        Stop: [
            {
                matcher: "",
                hooks: [
                    {
                        type: "command",
                        command: `${stopScript} ${opts.workspaceId}`,
                    },
                ],
            },
        ],
    };
}
/** Header comment for the hooks section, used for identification. */
exports.HOOKS_HEADER = "m-notes: auto-generated hooks";
